{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Kettu",
            "type": "shell",
            "command": "cargo",
            "args": [
                "build",
                "--bin",
                "kettu",
                "--manifest-path",
                "${workspaceFolder}/crates/kettu-cli/Cargo.toml"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": ["$rustc"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
